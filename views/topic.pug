extends layout
block content
  h1= display_name
  if articles
    p Information known to #{charName} about "#{display_name}":
    each article in content_array
      .articlecontainer
        .article #{article.content}

  else
    p There is no information available to you about '#{display_name}'
